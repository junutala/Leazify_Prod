import AppLayout from '@/components/AppLayout';
import LeasingClient from './components/LeasingClient';

export default function LeasingPage() {
  return (
    <AppLayout>
      <LeasingClient />
    </AppLayout>
  );
}
